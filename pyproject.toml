[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "gti_v2"
version = "0.1.0"
description = "GTI V2 Trading Intelligence Engine"
readme = "README.md"

authors = [
    { name = "Ankit Patel" }
]

license = { text = "Private Proprietary" }

requires-python = ">=3.12"

dependencies = []
